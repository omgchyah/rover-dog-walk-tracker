python manage.py makemigrations
python manage.py migrate
PYTHON MANAGE.PY GRAPH_MODELS > MODELS.DOT


# 1. Create a fresh project
npx create-expo-app@latest frontend --template blank-typescript

# 2. Go into the folder
cd frontend

# 3. Force the dependencies to match SDK 54
# This command tells Expo: "Fix all my versions to work with SDK 54"
npx expo install expo@~54.0.0 --fix