import Ionicons from '@expo/vector-icons/Ionicons';

const mapIcon = (category: string) => {
    switch (category) {
        case 'LODGING':
            return { name: 'hotel', color: '#3498db' }
    }
}